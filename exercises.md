IMPORTANT: Do ALL exercises inside ~/cli_sandbox to avoid changing important files. Start with:
1) mkdir -p ~/cli_sandbox
2) cd ~/cli_sandbox

1: Write something in a file called "difficulty.txt".
touch difficulty
echo "Hej" > dif
ficulty.txt 

2: Create a file called "pioneer.txt" and immediately delete it.
touch pioneer.txt
rm pioneer.tx

3: Move one level up (stay inside "cli_sandbox").
cd ..

4: Open a file in the terminal and see the content.
open (filename)
cat (filename

5: Create a file "beat.txt" containing: "ditch degree depression beat terminal". Then search recursively for "beat".
touch beat.txt
echo "ditch degree depression beat terminal" > beat.txt
grep -rn "beat2 .

6: Create a file called "discover.txt" containing these words (one per line): "ethics needle ethics bay ethics". Then count the lines.
nano discover.txt
(skriv ordene)
CTRL + X
Y
ENTER
wc -l discover.txt

7: Output what's in the directory.
ls

8: Create a file "nerve.txt" containing: "depression proportion depression shot depression". Then output the sorted lines.
nano nerve.txt
(skriv ordene)
CTRL + X
Y
ENTER
sort nerve.txt

9: Create a file called "pen.txt" and immediately delete it.
touch pen.txt
rm pen.tx

10: Create two directories "proportion" and "appreciate" and move into "appreciate".
mkdir proportion aprreciate
cd appreciate

11: Create a folder named "cli_sandbox".
mkdir cli_sandbox

12: Create a file "difficulty.txt" containing: "appreciate recommend evaluate difficulty terminal". Then search recursively for "difficulty".
touch difficulty.txt
echo appreciate recommend evaluate difficulty terminal > difficulty.txt
grep -rn "difficulty"

13: Output the full path you are on.
pwd

14: Use: echo "pray on advice recommend experienced" > "honor.txt" to create or overwrite "honor.txt".
echo pray on advice recommend experienced > honor.txt

15: Output the full path you are on.
pwd

16: Output the full path you are on.
pwd

17: Use: echo "proportion regret panel rain ethics" > "on.txt" to create or overwrite "on.txt".
echo proportion regret panel rain ethics > on.txt

18: Create a file "nerve.txt" containing: "sister recession sister rain sister". Then output the sorted lines.
echo "sister recession sister rain sister" > nerve.txt
sort nerve.txt

19: Move one level up (stay inside "cli_sandbox")
cd ..

20: If already inside "cli_sandbox", create a directory "salvation" and move into the directory.
mkdir salvation
cd salvation 

21: Change directory to "cli_sandbox".
cd ..

22: Create a file called "relative.txt".
touch relative.txt

23: Create a file called "shallow.txt" containing these words (one per line): "rain ethics rain relative rain". Then count the lines.
nano shallow.txt
(skriv ordene)
CTRL + X
Y
ENTER
wc -l shallow.txt

24: Open a file in the terminal and see the content.
cat (filename)

25: Move two folders up (but never leave "cli_sandbox").

26: Output hidden files.

27: Output the sizes of the files in the directory.

28: If there are any directories in the current folder, then move inside any one of them.

29: Output the full path you are on.

30: Change directory to "cli_sandbox".

31: Create a file "pen.txt" containing: "ethics advantage ethics hill ethics". Then output the sorted lines.

32: Delete the last file you created.

33: Create a directory "terminal" and then delete it.

34: If already inside "cli_sandbox", create a directory "sister" and move into the directory.

35: Delete the last folder you created.

36: Create two directories "regret" and "advantage" and move into "advantage".

37: Move two folders up (but never leave "cli_sandbox").

38: Open a file in the terminal and see the content.

39: Move two folders up (but never leave "cli_sandbox").

40: Open a file in the terminal and see the content.

41: Create two directories "panel" and "ditch" and move into "ditch".

42: If there are any directories in the current folder, then move inside any one of them.

43: Create a file called "nerve.txt" containing these words (one per line): "shot depression shot regret shot". Then count the lines.

44: Output the full path you are on.

45: Change directory to "cli_sandbox".

46: Create a file "degree.txt" containing: "recession depression degree cousin discover". Then search recursively for "degree".

47: Move two folders up (but never leave "cli_sandbox").

48: If already inside "cli_sandbox", create a directory "shot" and move into the directory.

49: Write something in a file called "experienced.txt".

50: Use: echo "support relative rider plane pen" > "support.txt" to create or overwrite "support.txt".

51: Create a file "support.txt" containing: "recession nerve excuse support shallow". Then search recursively for "support".

52: Output the full path you are on.

53: Create a directory "architecture" and then delete it.

54: Output the sizes of the files in the directory.

55: Write something in a file called "pioneer.txt".

56: Delete the last folder you created.

57: Output the content of any file in the directory.

58: If there are any directories in the current folder, then move inside any one of them.

59: If already inside "cli_sandbox", create a directory "proportion" and move into the directory.

60: Output what's in the directory.

61: Output who the active user is.

62: Create two directories "plane" and "pioneer" and move into "pioneer".

63: Create a file called "depression.txt" containing these words (one per line): "speculate evaluate speculate sister speculate". Then count the lines.

64: Output the full path you are on.

65: Create a file called "identity.txt" containing these words (one per line): "shallow medal shallow ditch shallow". Then count the lines.

66: Create a file "medal.txt" containing: "medal recommend difficulty difficulty medal". Then search recursively for "medal".

67: Output the sizes of the files in the directory.

68: Output the content of any file in the directory.

69: Create a file called "pen.txt".

70: Use: echo "advantage surprise architecture stock pen" > "panel.txt" to create or overwrite "panel.txt".

71: Create two directories "nerve" and "advantage" and move into "advantage".

72: Change directory to "cli_sandbox".

73: Create a file "surprise.txt" containing: "cousin recession relative surprise medal". Then search recursively for "surprise".

74: Move one level up (stay inside "cli_sandbox").

75: Create a file called "medal.txt" and immediately delete it.

76: Move two folders up (but never leave "cli_sandbox").

77: Output what's in the directory.

78: Create a file called "experienced.txt".

79: Create two directories "identity" and "shallow" and move into "shallow".

80: Use: echo "salvation advantage pen identity cousin" > "ditch.txt" to create or overwrite "ditch.txt".

81: Delete the last folder you created.

82: Output the sizes of the files in the directory.

83: Use: echo "banish speculate rain cater nerve" > "evaluate.txt" to create or overwrite "evaluate.txt".

84: Change directory to "cli_sandbox".

85: Output what's in the directory.

86: Output what's in the directory.

87: Output the content of any file in the directory.

88: Create a file called "speculate.txt" containing these words (one per line): "honor beat honor recession honor". Then count the lines.

89: Output the sizes of the files in the directory.

90: Create a file called "interference.txt".

91: Output the sizes of the files in the directory.

92: Move two folders up (but never leave "cli_sandbox").

93: Create a file called "pioneer.txt" and immediately delete it.

94: Output the sizes of the files in the directory.

95: Write something in a file called "terminal.txt".

96: Create a folder named "cli_sandbox".

97: Write something in a file called "relative.txt".

98: Create two directories "correspondence" and "appreciate" and move into "appreciate".

99: Output the full path you are on.

100: Create two directories "panel" and "speculate" and move into "speculate".

101: Create a file called "experienced.txt" containing these words (one per line): "pioneer speculate pioneer sister pioneer". Then count the lines.

102: Create a file called "ethics.txt".

103: Delete the last folder you created.

104: Create a directory "regret" and then delete it.

105: Create a file called "needle.txt" and immediately delete it.

106: Output who the active user is.

107: Delete the last file you created.

108: Create a directory "hill" and then delete it.

109: Open a file in the terminal and see the content.

110: Output hidden files.

111: Output who the active user is.

112: Create a file called "identity.txt" and immediately delete it.

113: If there are any directories in the current folder, then move inside any one of them.

114: Output what's in the directory.

115: Output the full path you are on.

116: Create a file called "depression.txt" and immediately delete it.

117: If there are any directories in the current folder, then move inside any one of them.

118: Move one level up (stay inside "cli_sandbox").

119: Create a file called "relative.txt" and immediately delete it.

120: Delete the last file you created.

121: Output hidden files.

122: Create a file "sister.txt" containing: "appreciate cousin appreciate nerve appreciate". Then output the sorted lines.

123: Create a file "rider.txt" containing: "depression cousin cousin shot rider". Then search recursively for "rider".

124: If already inside "cli_sandbox", create a directory "ethics" and move into the directory.

125: Create a file "cater.txt" containing: "recommend advice recommend panel recommend". Then output the sorted lines.

126: Create a file called "panel.txt".

127: Create a file called "medal.txt".

128: Delete the last folder you created.

129: Change directory to "cli_sandbox".

130: Move two folders up (but never leave "cli_sandbox").

131: Output hidden files.

132: If there are any directories in the current folder, then move inside any one of them.

133: Create two directories "experienced" and "nerve" and move into "nerve".

134: Delete the last file you created.

135: Use: echo "pen sister surprise advice needle" > "salvation.txt" to create or overwrite "salvation.txt".

136: Delete the last folder you created.

137: Create a file called "shot.txt".

138: Move one level up (stay inside "cli_sandbox").

139: Create a file "recommend.txt" containing: "rider identity rider advice rider". Then output the sorted lines.

140: Create a folder named "cli_sandbox".

141: Create a directory "honor" and then delete it.

142: Output what's in the directory.

143: Output hidden files.

144: Output who the active user is.

145: Change directory to "cli_sandbox".

146: Write something in a file called "evaluate.txt".

147: Use: echo "panel rider regret experienced ethics" > "architecture.txt" to create or overwrite "architecture.txt".

148: Create a file "medal.txt" containing: "depression pen depression identity depression". Then output the unique lines.

149: Create a file called "regret.txt" and immediately delete it.

150: Delete the last folder you created.

151: Create a folder named "cli_sandbox".

152: Delete the last file you created.

153: Create a file "recession.txt" containing: "relative recession rider support plane". Then search recursively for "recession".

154: Create a directory "panel" and then delete it.

155: Create a folder named "cli_sandbox".

156: Create a directory "difficulty" and then delete it.

157: If there are any directories in the current folder, then move inside any one of them.

158: Delete the last folder you created.

159: Move one level up (stay inside "cli_sandbox").

160: Create a file called "support.txt".

161: Open a file in the terminal and see the content.

162: Delete the last file you created.

163: Create a file called "needle.txt" and immediately delete it.

164: Create a directory "experienced" and then delete it.

165: Create two directories "degree" and "experienced" and move into "experienced".

166: Create two directories "salvation" and "difficulty" and move into "difficulty".

167: Delete the last file you created.

168: Move one level up (stay inside "cli_sandbox").

169: Change directory to "cli_sandbox".

170: Write something in a file called "shallow.txt".

171: Output the sizes of the files in the directory.

172: Output the full path you are on.

173: Use: echo "nerve needle experienced recommend speculate" > "experienced.txt" to create or overwrite "experienced.txt".

174: Move two folders up (but never leave "cli_sandbox").

175: Output who the active user is.

176: Move one level up (stay inside "cli_sandbox").

177: Create a directory "advantage" and then delete it.

178: Open a file in the terminal and see the content.

179: Output hidden files.

180: Delete the last folder you created.

181: Delete the last folder you created.

182: Output hidden files.

183: Output who the active user is.

184: Output the content of any file in the directory.

185: Write something in a file called "advice.txt".

186: Create a directory "interference" and then delete it.

187: Output hidden files.

188: Output the content of any file in the directory.

189: Output hidden files.

190: Delete the last folder you created.

191: Use: echo "shallow stock medal depression shot" > "beat.txt" to create or overwrite "beat.txt".

192: Output hidden files.

193: Use: echo "degree bay terminal regret proportion" > "cousin.txt" to create or overwrite "cousin.txt".

194: Delete the last folder you created.

195: Open a file in the terminal and see the content.

196: Create a file "architecture.txt" containing: "support shot support terminal support". Then output the sorted lines.

197: If already inside "cli_sandbox", create a directory "beat" and move into the directory.

198: Create a file "ditch.txt" containing: "shot responsibility shot degree shot". Then output the sorted lines.

199: If already inside "cli_sandbox", create a directory "shot" and move into the directory.

200: Create two directories "excuse" and "experienced" and move into "experienced".

201: Output the full path you are on.

202: Change directory to "cli_sandbox".

203: Create a directory "rain" and then delete it.

204: Move one level up (stay inside "cli_sandbox").

205: Use: echo "responsibility advice speculate rain bay" > "cousin.txt" to create or overwrite "cousin.txt".

206: Create a folder named "cli_sandbox".

207: Output what's in the directory.

208: Create two directories "terminal" and "pray" and move into "pray".

209: Output the content of any file in the directory.

210: Create a file "experienced.txt" containing: "discover recommend honor relative experienced". Then search recursively for "experienced".

211: Use: echo "degree beat proportion rider advantage" > "salvation.txt" to create or overwrite "salvation.txt".

212: Move two folders up (but never leave "cli_sandbox").

213: Move one level up (stay inside "cli_sandbox").

214: Move two folders up (but never leave "cli_sandbox").

215: Create a file called "hill.txt" containing these words (one per line): "bay rain bay discover bay". Then count the lines.

216: Create a file called "identity.txt" containing these words (one per line): "relative correspondence relative banish relative". Then count the lines.

217: Output what's in the directory.

218: Output the content of any file in the directory.

219: Create a folder named "cli_sandbox".

220: Output who the active user is.

221: Create a folder named "cli_sandbox".

222: Change directory to "cli_sandbox".

223: Move one level up (stay inside "cli_sandbox").

224: Output the full path you are on.

225: Create a file "relative.txt" containing: "cousin relative medal cousin appreciate". Then search recursively for "relative".

226: Output hidden files.

227: Output hidden files.

228: Use: echo "experienced hill beat bay recession" > "excuse.txt" to create or overwrite "excuse.txt".

229: Create a file called "nerve.txt".

230: Output the sizes of the files in the directory.

231: Delete the last file you created.

232: Create a file "responsibility.txt" containing: "nerve appreciate nerve depression nerve". Then output the sorted lines.

233: Output the sizes of the files in the directory.

234: Write something in a file called "nerve.txt".

235: Output what's in the directory.

236: Move two folders up (but never leave "cli_sandbox").

237: Delete the last file you created.

238: Move two folders up (but never leave "cli_sandbox").

239: Create a file "ethics.txt" containing: "surprise on surprise identity surprise". Then output the unique lines.

240: Create two directories "identity" and "advantage" and move into "advantage".

241: Create a file called "support.txt" and immediately delete it.

242: Create a directory "surprise" and then delete it.

243: Output the content of any file in the directory.

244: Create a file called "plane.txt".

245: Output the full path you are on.

246: Move two folders up (but never leave "cli_sandbox").

247: Output the full path you are on.

248: Output the sizes of the files in the directory.

249: Output the sizes of the files in the directory.

250: Create a folder named "cli_sandbox".

251: Create a file "ethics.txt" containing: "experienced relative experienced banish experienced". Then output the unique lines.

252: If already inside "cli_sandbox", create a directory "relative" and move into the directory.

253: Create a file called "pioneer.txt" and immediately delete it.

254: Output what's in the directory.

255: Create a directory "relative" and then delete it.

256: Create a directory "nerve" and then delete it.

257: Create a file called "recession.txt" and immediately delete it.

258: Move two folders up (but never leave "cli_sandbox").

259: Create a file "discover.txt" containing: "ethics cater ethics difficulty ethics". Then output the sorted lines.

260: Create a directory "correspondence" and then delete it.

261: Create a file called "speculate.txt" and immediately delete it.

262: Output the content of any file in the directory.

263: Create two directories "relative" and "plane" and move into "plane".

264: Change directory to "cli_sandbox".

265: Move one level up (stay inside "cli_sandbox").

266: Output the content of any file in the directory.

267: If there are any directories in the current folder, then move inside any one of them.

268: Change directory to "cli_sandbox".

269: Create a file "pray.txt" containing: "bay bay bay speculate bay". Then output the unique lines.

270: Create a file "recommend.txt" containing: "stock excuse recommend surprise beat". Then search recursively for "recommend".

271: Write something in a file called "relative.txt".

272: Open a file in the terminal and see the content.

273: Write something in a file called "banish.txt".

274: Delete the last file you created.

275: Delete the last folder you created.

276: Create a file called "shot.txt" containing these words (one per line): "sister beat sister on sister". Then count the lines.

277: Delete the last folder you created.

278: Use: echo "depression pray plane pioneer speculate" > "discover.txt" to create or overwrite "discover.txt".

279: Create a file called "relative.txt".

280: Output hidden files.

281: Create a file "surprise.txt" containing: "salvation panel salvation discover salvation". Then output the sorted lines.

282: Move one level up (stay inside "cli_sandbox").

283: Use: echo "cater advice on cousin shallow" > "panel.txt" to create or overwrite "panel.txt".

284: Output the content of any file in the directory.

285: Create a file called "plane.txt" and immediately delete it.

286: Delete the last file you created.

287: Output the full path you are on.

288: Move two folders up (but never leave "cli_sandbox").

289: Move two folders up (but never leave "cli_sandbox").

290: If already inside "cli_sandbox", create a directory "depression" and move into the directory.

291: Create a file called "needle.txt" containing these words (one per line): "bay proportion bay cousin bay". Then count the lines.

292: Move one level up (stay inside "cli_sandbox").

293: Output what's in the directory.

294: Create a folder named "cli_sandbox".

295: Create a file called "depression.txt".

296: Create a file "rain.txt" containing: "honor nerve honor salvation honor". Then output the sorted lines.

297: If already inside "cli_sandbox", create a directory "surprise" and move into the directory.

298: Output the full path you are on.

299: Create a file "rain.txt" containing: "evaluate ethics evaluate salvation evaluate". Then output the sorted lines.

300: Create a file "on.txt" containing: "hill shot hill recommend hill". Then output the sorted lines.

301: Open a file in the terminal and see the content.

302: Output who the active user is.

303: Move one level up (stay inside "cli_sandbox").

304: Create a file called "cater.txt" containing these words (one per line): "hill medal hill depression hill". Then count the lines.

305: Create a file called "hill.txt".

306: Create a file "rider.txt" containing: "rider depression hill evaluate correspondence". Then search recursively for "rider".

307: Output the sizes of the files in the directory.

308: Delete the last folder you created.

309: Output hidden files.

310: Delete the last file you created.

311: Create a file called "identity.txt" and immediately delete it.

312: Output the full path you are on.

313: Create a file called "panel.txt" and immediately delete it.

314: Create two directories "terminal" and "terminal" and move into "terminal".

315: Open a file in the terminal and see the content.

316: Output what's in the directory.

317: If there are any directories in the current folder, then move inside any one of them.

318: Output hidden files.

319: Use: echo "medal bay relative hill honor" > "cater.txt" to create or overwrite "cater.txt".

320: Use: echo "honor banish panel nerve architecture" > "banish.txt" to create or overwrite "banish.txt".

321: Create a directory "salvation" and then delete it.

322: Create a file called "rain.txt" containing these words (one per line): "speculate support speculate proportion speculate". Then count the lines.

323: If there are any directories in the current folder, then move inside any one of them.

324: Delete the last file you created.

325: Open a file in the terminal and see the content.

326: Write something in a file called "difficulty.txt".

327: Use: echo "speculate advice pray banish sister" > "ethics.txt" to create or overwrite "ethics.txt".

328: Delete the last folder you created.

329: Output hidden files.

330: Create a file called "relative.txt" and immediately delete it.

331: Create a file called "ethics.txt" containing these words (one per line): "degree evaluate degree on degree". Then count the lines.

332: Create two directories "rider" and "discover" and move into "discover".

333: Delete the last folder you created.

334: If there are any directories in the current folder, then move inside any one of them.

335: Create a file "interference.txt" containing: "recommend ditch recommend beat recommend". Then output the sorted lines.

336: If already inside "cli_sandbox", create a directory "plane" and move into the directory.

337: If there are any directories in the current folder, then move inside any one of them.

338: Output hidden files.

339: Delete the last file you created.

340: Create a file "plane.txt" containing: "plane depression on degree terminal". Then search recursively for "plane".

341: Change directory to "cli_sandbox".

342: Create a file "experienced.txt" containing: "identity responsibility identity rain identity". Then output the sorted lines.

343: Output what's in the directory.

344: Output the full path you are on.

345: Create two directories "recession" and "hill" and move into "hill".

346: Move two folders up (but never leave "cli_sandbox").

347: Create a file called "appreciate.txt" containing these words (one per line): "regret medal regret appreciate regret". Then count the lines.

348: Open a file in the terminal and see the content.

349: If already inside "cli_sandbox", create a directory "hill" and move into the directory.

350: Delete the last file you created.

